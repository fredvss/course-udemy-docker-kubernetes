apiVersion: v1
kind: Service
metadata:
  labels:
    name: nginx-service-clusterip
  name: nginx-service-clusterip
  namespace: default
spec:
  selector:
    app: nginx
  ports:
  - name: http
    port: 8080
    targetPort: 80
  type: ClusterIP