apiVersion: v1
kind: Service
metadata:
  labels:
    name: nginx-service-loadbalancer
  name: nginx-service-loadbalancer
  namespace: default
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 80
  selector:
    app: nginx
  type: LoadBalancer