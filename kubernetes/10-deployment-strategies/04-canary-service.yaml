apiVersion: v1
kind: Service
metadata:
  labels:
    name: nginx-service-canary
  name: nginx-service-canary
  namespace: default
spec:
  selector:
    app: nginx-canary
  ports:
  - name: http
    port: 8080
    targetPort: 80
  type: LoadBalancer