apiVersion: v1
kind: Service
metadata:
  labels:
    name: nginx-service
  name: nginx-service
  namespace: default
spec:
  selector:
    app: nginx
  ports:
  - name: http
    port: 8080
    targetPort: 80
  type: ClusterIP