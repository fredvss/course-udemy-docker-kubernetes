apiVersion: v1
kind: Service
metadata:
  labels:
    name: nginx-service
  name: nginx-service
  namespace: default
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 80
  selector:
    app: nginx
  type: LoadBalancer